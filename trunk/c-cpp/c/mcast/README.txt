
该目录下的程序(mcli 和 mser)用于测试组播

mcli    用于向组播地址为 232.0.0.101 的组发送消息
mser    用于从组播地址为 232.0.0.101 的组获取消息


测试过程:
分别把这两个程序复制到两个板子上,
在其中一板子上运行:
  # ./mser ip

在另一板子上运行:
  # ./mcli ip

(这里的"ip"是板子的ip)


按正常情况下, mser应该接收并打印出消息, 但测试结果是接收不到!
